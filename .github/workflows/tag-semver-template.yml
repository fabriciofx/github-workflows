# SPDX-FileCopyrightText: Copyright (C) 2025 Fabrício Barros Cabral
# SPDX-License-Identifier: MIT
---
# yamllint disable rule:line-length
name: tag-semver-template
'on':
  workflow_call:
    inputs:
      working-directory:
        description: "Set the working directory"
        required: false
        default: "."
        type: string
defaults:
  run:
    shell: bash -xeuo pipefail {0}
permissions:
  contents: read
jobs:
  tag-semver-template:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          persist-credentials: false
      - name: Check tag name
        id: workflows
        run: |
          if [[ "${GITHUB_REF_NAME}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "cancel=false" >> "${GITHUB_OUPUT}"
          else
            echo "Tag '${GITHUB_REF_NAME}' has an invalid semver format.""
            echo "Please, use a valid format: ^[0-9]+\.[0-9]+\.[0-9]+$"
            echo "cancel=true" >> "${GITHUB_OUPUT}"
          fi
      - name: Cancel all active workflows
        if: steps.workflows.outputs.cancel == 'true'
        run: |
          curl -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/cancel
