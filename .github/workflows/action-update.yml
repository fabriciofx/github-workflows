# SPDX-FileCopyrightText: Copyright (C) 2025 Fabrício Barros Cabral
# SPDX-License-Identifier: MIT
---
name: action-update
'on':
  schedule:
    - cron: '0 2 * * *'
  workflow_dispatch:
defaults:
  run:
    shell: bash -xeuo pipefail {0}
permissions:
  contents: read
jobs:
  action-update:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          persist-credentials: false
      - name: Setup node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
      - name: Run renovate
        uses: renovatebot/github-action@e74772da243b99cc2b941d65604ef3bcb0a0b58e
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
